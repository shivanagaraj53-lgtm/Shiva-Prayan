# Prayan — release shrinking rules.
#
# The Flutter Gradle plugin contributes the engine's own keep-rules, so this
# file starts empty on purpose. Add rules here when a plugin needs reflection
# survived; do not disable shrinking to work around one.
